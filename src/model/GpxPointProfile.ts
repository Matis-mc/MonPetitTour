class GpxPointProfile {
    lat: number;
    lng: number;
    ele: number;

    constructor(lat: number, lng: number, ele: number) {
        this.lat = lat;
        this.lng = lng;
        this.ele = ele;
    }
}